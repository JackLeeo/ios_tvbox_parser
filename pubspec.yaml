name: ios_tvbox_parser
description: 原生视频解析播放器
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  flutter_spinkit: ^5.2.0
  hls_proplayer: ^0.1.5      # 支持弹幕、手势的原生播放器

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
